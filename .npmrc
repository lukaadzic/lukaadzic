# Ensure native dependencies are rebuilt for the target platform
rebuild=true
# Use exact versions to avoid compatibility issues
save-exact=true
# Ensure proper handling of optional dependencies
optional=false
# Force installation of all optional dependencies
include-optional=true
# Ensure native modules are rebuilt for target platform
target_platform=linux
target_arch=x64
